# SQL schemas

## Postgres

1. For the outbox_events table use [open-outbox.sql](./postgres/open-outbox.sql)
2. For manual/periodic maintenance create the maintenance indices using [maintenance.sql](./postgres/maintenance.sql)
